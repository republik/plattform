const covidAccessTokens = require('./covidAccessTokens')
const nameAndEmailBase64u = require('./nameAndEmailBase64u')

module.exports = {
  operations: {
    covidAccessTokens,
    nameAndEmailBase64u,
  },
}
