import { ElementConfigI } from '../../../custom-types'
import { Interaction } from '../../../../Typography'
import { TitleIcon } from '../../../../Icons'

export const config: ElementConfigI = {
  Component: {
    article: Interaction.H2,
  },
  button: { icon: TitleIcon },
}
