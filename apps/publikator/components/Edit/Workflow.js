import { Phase } from '../Repo/Phases'

const WorkflowOverlay = () => <div>TBD</div>

// on click: workflow overlay
export const PhaseSummary = () => (
  <div>
    <Phase phase={{ label: 'Peer', color: 'gold' }} />
  </div>
)
