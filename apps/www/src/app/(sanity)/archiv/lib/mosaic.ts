// Front-Teasers are built to run the full width of the front, so the archive
// lays them out at that width and shrinks the result into its column.
export const RENDER_WIDTH = 1175

// Below this the mosaic is a single column and nothing is scaled — at that
// width the teasers' own mobile styling is what we want.
export const UNSCALED_BELOW = 640
