import * as React from 'react'
import { User } from '../../../types/admin'
import { Checkbox } from '@project-r/styleguide'

export interface RoleFormProps {
  roles: string[]
  user: User
}

export default ({ roles, user }: RoleFormProps) => {
  return <div />
}
