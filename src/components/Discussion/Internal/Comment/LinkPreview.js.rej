diff a/src/components/Discussion/Internal/Comment/LinkPreview.js b/src/components/Discussion/Internal/Comment/LinkPreview.js	(rejected hunks)
@@ -4,3 +4,7 @@ export default ({ preview }) => {
-  return preview ? (<>
-    {preview.linkPreview && <img src={preview.linkPreview.imageUrl} width='200' />}
-  </>) : null
+  return preview ? (
+    <>
+      {preview.linkPreview && (
+        <img src={preview.linkPreview.imageUrl} width='200' />
+      )}
+    </>
+  ) : null
