import { IconExplorer } from '@republik/docs'

# Icons

### `@republik/icons`

import * as icons from '@republik/icons'

<IconExplorer
  icons={Object.entries(icons).map(([k, v]) => ({ title: k, Icon: v }))}
/>
