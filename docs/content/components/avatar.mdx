---
title: 'Avatar (Demo)'
---

import { ComponentDemo, PropsTable } from '@/components'
import * as Props from '@republik/ui-docs-demo?props'

# Avatar (Demo)

This is just a demo to show that we can import packages with TSX and [CSS Modules](https://nextjs.org/docs/basic-features/built-in-css-support#adding-component-level-css) directly 🎉

{/* <ComponentDemo file='demos/avatar.tsx' /> */}

☝️ To render a component directly from a file _and_ also show the code of that file directly below, use the `ComponentDemo` component, like so:

```tsx
<ComponentDemo file='demos/avatar.tsx' />
```

## Avatar Props

<PropsTable props={Props.Avatar} />
